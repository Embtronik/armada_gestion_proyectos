server:
  port: 8092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/proyectos
    username: proyectos_user
    password: Arm@d@2025
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
    open-in-view: false
  mail:
    host: ${SMTP_HOST:smtp.zoho.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:comercial@gpsrealtime.com.co}
    password: ${SMTP_PASS:ai10rnYu@}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true
    default-encoding: UTF-8
  freemarker:
    template-loader-path: classpath:/templates/
    charset: UTF-8
    prefer-file-system-access: false
  flyway:
    enabled: true
    locations: classpath:db/migration

  rabbitmq:              
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:admin}
    password: ${SPRING_RABBITMQ_PASSWORD:admin123}

notificaciones:
  correo:
    default-from: ${SMTP_FROM:comercial@gpsrealtime.com.co}
  rabbitmq:
    exchange: notificaciones.exchange
    routing-key: notificaciones.email
    queue: notificaciones.queue
    dlq: notificaciones.queue.dlq
    max-retry: 5
